# Base de datos local (para desarrollo sin Docker)
DATABASE_URL=postgresql://usuario:password@localhost:5432/cafeteria

# Credenciales de PostgreSQL para Docker
# Copia este archivo a .env y cambia estos valores antes de hacer docker-compose up
POSTGRES_DB=cafeteria
POSTGRES_USER=tu_usuario
POSTGRES_PASSWORD=tu_password_segura
